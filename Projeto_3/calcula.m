function aresta = calcula(pf,peso)

    % verifica a distancia para cada aresta adjunta a menor ganha
    [d GPS(1) GPS(2)] = dpr(GPS(1),GPS(1),a,b);
    
    % se ela atravessar o vertesse, entao ela muda de vertice
    

end